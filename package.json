{
  "name": "@stereck/getfresh",
  "version": "1.1.0",
  "description": "Node.js bridge to access inofficial Fresh Energy API to read consumption data and write it to MQTT",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --ignore-path .gitignore --ext .js --ext .jsx ."
  },
  "author": "Stephan Recknagel",
  "license": "Apache-2.0",
  "dependencies": {
    "dateformat": "^3.0.3",
    "mqtt": "^2.18.8",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0"
  }
}
